<Window x:Class="Milionar.MainWindow"
        x:Name="mainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Milionar"
        mc:Ignorable="d"
        ResizeMode="CanMinimize"
        Background="#FF21272C"
        Title="MainWindow" Height="705" Width="1200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="547*"/>
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="149*"/>
            <ColumnDefinition Width="25*"/>
        </Grid.ColumnDefinitions>

        <Grid.Background>
            <ImageBrush ImageSource="background.jpg" Stretch="UniformToFill" Opacity="0.6"/>
        </Grid.Background>

        <Frame NavigationUIVisibility="Hidden" x:Name="mainFrame" Grid.ColumnSpan="3" Grid.RowSpan="3"></Frame>
    </Grid>
</Window>
